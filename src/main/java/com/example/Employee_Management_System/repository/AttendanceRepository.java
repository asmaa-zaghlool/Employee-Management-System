package com.example.Employee_Management_System.repository;

import java.util.*;
import com.example.Employee_Management_System.entity.Attendance;

public class AttendanceRepository {

//    private final Map<String, Attendance> db = new HashMap<>();
//
//    public Attendance save(Attendance attendance) {
//        db.put(attendance.getId(), attendance);
//        return attendance;
//    }
//
//    public List<Attendance> findByEmployeeId(String employeeId) {
//        return db.values().stream()
//                .filter(a -> a.getEmployeeId().equals(employeeId))
//                .toList();
//    }
//
//    public List<Attendance> findByEmployeeId(String employeeId) {
//        return db.values().stream()
//                .filter(a -> a.getEmployeeId().equals(employeeId))
//                .toList();
//    }
}
